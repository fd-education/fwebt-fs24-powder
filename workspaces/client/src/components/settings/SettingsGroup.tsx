import React from 'react';
import { ScreenModeToggle } from './ScreenModeToggle';

export const SettingsGroup = () => {
  return (
    <div className={`my-5`}>
      <ScreenModeToggle />
    </div>
  );
};
